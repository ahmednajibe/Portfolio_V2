/**
 * @deprecated Use `useAnimation` from '~/composables/useAnimation' instead.
 * Kept as a thin re-export for backward compatibility.
 */
export { useAnimation as useScrollReveal } from './useAnimation'
